# chunk 策略服务任务

[返回全局摘要](../README.md) · [返回本组：上下文工程与 RAG](README.md)


## Rule
chunk 大小、边界和元数据应服务具体任务，而不是使用固定默认值。

## Why
不同任务需要不同粒度。问答需要可定位证据，摘要需要完整段落，流程任务需要保留步骤顺序。

## Optimize
- 按标题、段落、表格、代码块或业务对象切分。
- 保留文档层级、时间、版本和权限元数据。
- 避免把一个完整语义单元切断。

## Verify
- 抽样检查 chunk 是否语义完整。
- 用目标任务查询测试召回效果。
- 统计答案所需信息是否跨越过多 chunk。

## References
- 文档结构规范
- 业务对象模型
- 检索命中分析

---

[返回全局摘要](../README.md) · [返回本组：上下文工程与 RAG](README.md)
